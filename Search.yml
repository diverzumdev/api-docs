info:
  name: Search
  type: http
  seq: 11

http:
  method: GET
  url: "{{base_url}}/search?query=spa"
  params:
    - name: query
      value: spa
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
