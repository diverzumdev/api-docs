info:
  name: Verification methods
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url_v2}}/auth/verification_methods/{{learning_region_id}}"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
