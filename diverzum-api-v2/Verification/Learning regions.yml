info:
  name: Learning regions
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url_v2}}/auth/learning_regions"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
