info:
  name: Check document
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url_v2}}/auth/user/check-document"
  body:
    type: text
    data: ""
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
