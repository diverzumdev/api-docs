info:
  name: Update Learning region
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url_v2}}/auth/user/learning_region"
  body:
    type: json
    data: "{\r

      \    \"learning_region_id\": 1\r

      }"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
