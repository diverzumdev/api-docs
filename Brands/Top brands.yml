info:
  name: Top brands
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/favorites/top-brands"
  headers:
    - name: region
      value: en
      disabled: true
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
