info:
  name: Update Bug Report
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{base_url}}/auth/user-bug-report/71"
  body:
    type: form-urlencoded
    data:
      - name: description
        value: ez egy test az éles szerveren
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
