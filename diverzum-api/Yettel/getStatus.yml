info:
  name: getStatus
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/yettel/status?user_id=MTg3NTEy"
  params:
    - name: user_id
      value: MTg3NTEy
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
