info:
  name: Login
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/auth/login"
  body:
    type: multipart-form
    data:
      - name: email
        type: text
        value: development+1@diverzum.hu
      - name: password
        type: text
        value: asdasd
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
